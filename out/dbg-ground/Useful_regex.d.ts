export declare class ASP_REGEX {
    static FACT_REGEX: string;
    /** Group that matches the fact */
    static FACT_REGEX_MATCHING_GROUP: string;
    /** Regular expression that matches a variable */
    static VARIABLE_REGEX: string;
    private static MULTILINE;
    static FACT_PATTERN: RegExp;
    static VARIABLE_PATTERN: RegExp;
    static COMMENT_PATTERN: RegExp;
    static AGGREGATE_PATTERN: RegExp;
}
